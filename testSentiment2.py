import old_sentiment_mod as s
import unittest 

##s.showWord()


print s.sentiment("Structural height in technical usage is the height to the highest architectural detail on building from street-level.")


print s.sentiment(" Many applications pack all their data files into a single file called an archive file")
print s.sentiment("Create a new fileChange the access permissions and attributes of a fileOpen a file, which makes the file contents available to the programRead data from a fileWrite data to a file")
print s.sentiment("One such file manager is flist, which was first used in 1981 on the Conversational Monitor System")
print s.sentiment("These timings are somewhat altered in mode, as although it uses the same line rate")
print s.sentiment("VGA BNC connectors An alternative method of connecting VGA devices that maintains very high signal quality is the BNC connector")
print s.sentiment ("Multi-coloured portrait of a far eastern courtesan with elaborate hair ornamentation, colourful robe like garment, and a border depicting marshland waters and reeds.")
print s.sentiment("")
